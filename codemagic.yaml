workflows:
  ios-testflight:
    name: Pilateacher iOS TestFlight
    max_build_duration: 60
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: 필라티쳐

    environment:
      node: 22
      xcode: latest
      cocoapods: default
      groups:
        - signing
      vars:
        XCODE_SCHEME: "App"
        BUNDLE_ID: "com.pilateacher.app"

    scripts:
      - name: Install npm packages
        script: npm install

      - name: Build web
        script: npm run build

      - name: Capacitor sync ios
        script: npx cap sync ios

      - name: Install pods if needed
        script: |
          if [ -f ios/App/Podfile ]; then
            cd ios/App
            pod install
          else
            echo "Podfile not found - using Swift Package Manager, skipping"
          fi

      - name: Set up code signing
        script: |
          set -e
          keychain initialize
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --certificate-key=@env:CERTIFICATE_PRIVATE_KEY --create
          keychain add-certificates
          xcode-project use-profiles --project "$CM_BUILD_DIR/ios/App/App.xcodeproj"

      - name: Set build number
        script: |
          cd $CM_BUILD_DIR/ios/App
          agvtool new-version -all $BUILD_NUMBER

      - name: Build ipa
        script: |
          cd $CM_BUILD_DIR
          if [ -d ios/App/App.xcworkspace ]; then
            xcode-project build-ipa --workspace "ios/App/App.xcworkspace" --scheme "$XCODE_SCHEME"
          else
            xcode-project build-ipa --project "ios/App/App.xcodeproj" --scheme "$XCODE_SCHEME"
          fi

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
