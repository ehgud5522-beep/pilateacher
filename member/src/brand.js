/**
 * 회원에게 이 앱은 **센터의 앱**이다.
 *
 * "필라티쳐" 는 강사가 쓰는 제품의 이름이고 회원은 그 이름을 모른다. 회원이
 * 아는 것은 자기가 다니는 센터뿐이라, 화면에도 그 이름이 서야 한다.
 *
 * ── 왜 상수인가 ──
 * 투영 문서에는 지점 이름(`locationName`)은 있어도 센터 이름은 없고, 무엇보다
 * **로그인 화면에는 아직 아무 데이터도 없다** -- 번호를 넣기 전이다. 그 화면에
 * 이름을 세우려면 상수일 수밖에 없다.
 *
 * ── 두 번째 센터가 생기면 ──
 * 그때는 이 상수로 안 된다. 투영에 `organizationName` 을 더하고(member-view.js
 * 의 허용 목록) 로그인 화면만 중립적인 문구로 두면 된다. 지금 그렇게 만들지
 * 않는 것은, 쓰지 않을 갈래를 미리 만들면 그것이 맞는지 아무도 확인하지 못한
 * 채로 남기 때문이다.
 */

/** 1단계는 이 센터 전용이다 (확정 8번: 반송점만). */
export const CENTRE_NAME = "보니따필라테스";
